// 批量导出图表，按需使用
const icons = {
  lingyu: require('./lingyu.svg').default,
  rank1: require('./rank1.svg').default,
  rank2: require('./rank2.svg').default,
  rank3: require('./rank3.svg').default,
  rank4: require('./rank4.svg').default,
  a: require('./a.svg').default,
  b: require('./b.svg').default,
  c: require('./c.svg').default,
  d: require('./d.svg').default,
  s: require('./s.svg').default,
};

export default icons;